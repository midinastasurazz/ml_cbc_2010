addpath(genpath(pwd));
% start up the code. Adds evetything to the directory 
attribs = 1:45;
[examples, targets] = loaddata('cleandata_students.txt'); % loads the data
% to restore default path uncomment below
% restoredefaultpath;
