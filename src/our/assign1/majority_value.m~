function [ maj ] = majority_value( targets )
%MAJORITY_VALUE Summary of this function goes here
%   Detailed explanation goes here
    
%   mode returns the most frequent element in an array(targets)
    %maj = emolab2str(mode(targets));
    maj = mode(targets);
end